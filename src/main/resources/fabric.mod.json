{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "${name}",
  "description": "${description}",
  "authors": [${authors}],
  "contact": {
    "homepage": "https://modrinth.com/mod/Hardcore_Disabled_Fix",
    "sources": "https://github.com/WJiangzhi/Hardcore_Disabled_Fix",
    "issues": "https://github.com/WJiangzhi/Hardcore_Disabled_Fix/issues"
  },
  "license": "${license}",
  "icon": "icon.png",
  "environment": "client",
  "entrypoints": {
  },
  "mixins": [
    "${id}.mixins.json"
  ],
  "depends": {
    "fabricloader": "*",
    "fabric-api": ">=${fabricVersion}",
    "minecraft": "${fabric_mc_version_range}"
  }
}
